@page
@model FabrikaTakipPaneli.Pages.Drivers.IndexModel
@{
    ViewData["Title"] = "Sürücüler";
}

<div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
    <h1 class="mb-0">Sürücüler</h1>
    <a asp-page="Create" class="btn btn-primary">
        <i class="bi bi-plus-lg"></i> Yeni Sürücü
    </a>
</div>

<form method="get" class="row row-cols-lg-auto g-2 align-items-center mb-3">
    <div class="col-12">
        <div class="input-group">
            <span class="input-group-text"><i class="bi bi-search"></i></span>
            <input type="text" name="SearchTerm" value="@Model.SearchTerm" class="form-control" placeholder="Ad veya TC Kimlik No'ya göre ara..." />
        </div>
    </div>
    <div class="col-12">
        <button type="submit" class="btn btn-outline-secondary">Ara</button>
        <a asp-page="Index" class="btn btn-outline-secondary"><i class="bi bi-x-lg"></i> Temizle</a>
    </div>
</form>

<div class="data-card">
    <table class="table">
        <thead>
            <tr>
                <th>Ad Soyad</th>
                <th>TC Kimlik No</th>
                <th>Telefon</th>
                <th>Ehliyet No</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Drivers.Count == 0)
            {
                <tr><td colspan="5" class="data-card-empty">Sürücü bulunamadı.</td></tr>
            }
            @foreach (var driver in Model.Drivers)
            {
                <tr>
                    <td>@driver.FullName</td>
                    <td class="font-mono">@driver.TCKimlikNo</td>
                    <td>@driver.Phone</td>
                    <td>@driver.LicenseNumber</td>
                    <td class="text-end">
                        <a asp-page="Edit" asp-route-id="@driver.Id" class="btn btn-sm btn-outline-secondary" title="Düzenle"><i class="bi bi-pencil"></i></a>
                        <a asp-page="Delete" asp-route-id="@driver.Id" class="btn btn-sm btn-outline-danger" title="Sil"><i class="bi bi-trash"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<partial name="_Pagination" model="@(new FabrikaTakipPaneli.Pages.Shared.PaginationViewModel
{
    CurrentPage = Model.PageIndex,
    TotalPages = Model.TotalPages,
    PageName = "Index",
    RouteData = new Dictionary<string, string?> { ["SearchTerm"] = Model.SearchTerm }
})" />
